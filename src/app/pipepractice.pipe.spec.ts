import { PipepracticePipe } from './pipepractice.pipe';

describe('PipepracticePipe', () => {
  it('create an instance', () => {
    const pipe = new PipepracticePipe();
    expect(pipe).toBeTruthy();
  });
});
